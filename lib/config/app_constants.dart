/// App-wide constants
class AppConstants {
  static const String schoolLocationType = 'SCHOOL_LOCATION';

  // ===== Location Tracking Intervals =====
  static const Duration socketUpdateInterval = Duration(seconds: 5);
  static const Duration databaseUpdateInterval = Duration(seconds: 30);
}
